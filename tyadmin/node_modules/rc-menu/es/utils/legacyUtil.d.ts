import { MotionType, TransitionNameType, OpenAnimation, MenuMode } from '../interface';
interface GetMotionProps {
    motion?: MotionType;
    defaultMotions?: Partial<{
        [key in MenuMode | 'other']: MotionType;
    }>;
    openAnimation?: OpenAnimation;
    openTransitionName?: TransitionNameType;
    prefixCls?: string;
}
interface GetMotionState {
    switchingModeFromInline: boolean;
}
export declare function getMotion({ prefixCls, motion, defaultMotions, openAnimation, openTransitionName, }: GetMotionProps, { switchingModeFromInline }: GetMotionState, menuMode: MenuMode): MotionType;
export {};
