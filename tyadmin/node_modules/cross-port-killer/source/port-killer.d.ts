export declare class Killer {
    private readonly platform;
    private platforms;
    constructor(platform: string);
    kill(port: number | string): Promise<string[] | null>;
    killByPid(pid: string): Promise<string[]>;
    killByPids(pids: string[]): Promise<string[]>;
    private darwin;
    private linux;
    private win32;
    private parse;
}
