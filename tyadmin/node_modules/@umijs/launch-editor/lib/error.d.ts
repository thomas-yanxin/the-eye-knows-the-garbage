export default class EditorError extends Error {
    editor: string | string[];
    success?: boolean;
    code?: string;
    constructor({ editor, code, success, }: {
        editor?: string;
        code?: string;
        success?: boolean;
    }, ...params: any[]);
}
