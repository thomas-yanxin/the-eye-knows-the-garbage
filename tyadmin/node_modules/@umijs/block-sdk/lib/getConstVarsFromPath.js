"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = void 0;

function _react() {
  const data = _interopRequireDefault(require("react"));

  _react = function _react() {
    return data;
  };

  return data;
}

function _utils() {
  const data = require("@umijs/utils");

  _utils = function _utils() {
    return data;
  };

  return data;
}

function _uppercamelcase() {
  const data = _interopRequireDefault(require("uppercamelcase"));

  _uppercamelcase = function _uppercamelcase() {
    return data;
  };

  return data;
}

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

const toLower = _utils().lodash.toLower,
      camelCase = _utils().lodash.camelCase;

function stripFirstSlash(path) {
  if (path.charAt(0) === '/') {
    return path.slice(1);
  }

  return path;
}

var _default = path => {
  let ROUTE_PATH = path;

  if (path.includes('-') || path.includes('_')) {
    ROUTE_PATH = toLower(path);
  }

  const PAGE_NAME = ROUTE_PATH.split('/').filter(str => str).pop() || 'NewPage';
  const BLOCK_NAME = stripFirstSlash(ROUTE_PATH).replace(/\//g, 'And');
  return new Map([['ROUTE_PATH', toLower(ROUTE_PATH)], // [XXX][_UPPER]_CAMEL_CASE 需要在 XXX 之前，
  // 因为先替换 XXX 会修改 [XXX][_UPPER]_CAMEL_CASE 里的 XXX
  ['BLOCK_NAME_CAMEL_CASE', camelCase(BLOCK_NAME)], ['BLOCK_NAME', toLower(BLOCK_NAME)], ['PAGE_NAME_UPPER_CAMEL_CASE', (0, _uppercamelcase().default)(PAGE_NAME)], ['PAGE_NAME', toLower(PAGE_NAME)]]);
};

exports.default = _default;