"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = _default;

function _react() {
  const data = _interopRequireDefault(require("react"));

  _react = function _react() {
    return data;
  };

  return data;
}

function _umi() {
  const data = require("umi");

  _umi = function _umi() {
    return data;
  };

  return data;
}

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

/**
 *  C:\GitHub\ant-design-pro\src\pages\Welcome\index.tsx
 * --->
 *   Welcome\index.tsx
 *  用与将路径变化为相对路径
 *  */
const winPath = _umi().utils.winPath,
      createDebug = _umi().utils.createDebug;

const debug = createDebug('umiui:UmiUI:block');

function _default({
  payload,
  api,
  success
}) {
  const _ref = payload,
        targetPath = _ref.path;
  const relativePagePath = winPath(targetPath).replace(api.paths.absPagesPath, '').replace(api.paths.absSrcPath, '').replace(/\//g, '/').replace(/\/\//g, '/');
  debug('getRelativePagesPath', targetPath, api.paths.absPagesPath);
  debug('relativePagePath', relativePagePath);
  success({
    data: relativePagePath,
    success: true
  });
}