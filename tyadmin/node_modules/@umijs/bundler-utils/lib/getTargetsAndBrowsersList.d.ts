import { IConfig, ITargets, IBundlerConfigType } from '@umijs/types';
interface IOpts {
    config: IConfig;
    type: IBundlerConfigType;
}
export default function ({ config, type }: IOpts): {
    targets: ITargets;
    browserslist: any;
};
export {};
