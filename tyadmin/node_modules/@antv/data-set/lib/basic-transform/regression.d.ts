import { RowData } from '../helper';
export interface RegressionOptions {
    as?: [string, string];
    method?: 'linear' | 'exponential' | 'logarithmic' | 'power' | 'polynomial';
    fields: [string, string];
    bandwidth?: number;
    extent?: [number, number];
}
export declare function regression(rows: RowData[], options: RegressionOptions): RowData[];
