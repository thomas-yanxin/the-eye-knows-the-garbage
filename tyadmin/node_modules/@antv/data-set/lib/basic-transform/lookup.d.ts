import { RowData } from '../helper';
export interface LookupParams {
    from: RowData[];
    key: string;
    field: string;
    values?: string[];
    as?: string[];
    default?: any;
}
export declare function lookup(rows: RowData[], options: LookupParams): RowData[];
